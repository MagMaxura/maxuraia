// Este archivo es un shim para jQuery.
// Asume que jQuery ya ha sido cargado globalmente (por ejemplo, a través de un CDN en index.html).
// Exporta window.jQuery para que los módulos que lo importen puedan acceder a él.
export default window.jQuery;